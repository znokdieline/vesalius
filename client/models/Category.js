class Category {
    constructor( id, title, color, SetScreen ) {
        this.id = id;
        this.title = title;
        this.color = color;
        this.SetScreen= SetScreen;
    }
};

export default Category;

