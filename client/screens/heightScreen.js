import React from 'react';
import { View, Text, StyleSheet, } from 'react-native';

const heightScreen = props => {
    return(
        <View>
            <Text> Estimate height </Text>
        </View>
    );
};

const styles = StyleSheet.create({});

export default heightScreen;